# WAAN v2.3.1

## What changed

- Carried forward `v2.3.0` release improvements:
  - Added release-enforced performance budgets for render/search/sync proxy paths (`check:perf-budgets`) and wired the gate into tagged release CI.
  - Expanded relay reliability diagnostics with explicit fallback reasons (`lastSyncPathReason`) and slow-sync operator hints in banner/progress surfaces.
  - Added guided recovery actions in degraded relay states: `Reconnect`, `Resync`, and `Export diagnostics`.
  - Hardened recovery behavior and state transitions:
    - `Reconnect` now performs a real restart (`stop` then `start`) when relay is already running.
    - Recovery action visibility now follows current runtime state/sync signals and avoids stale-error presentation.
    - Recovery controls now disable immediately when controls lock to prevent duplicate overlapping operations.
  - Expanded release-blocking regression coverage for relay transitions, export integrity, and recovery UI behavior.
- Updated docs to clarify tagged release requirements for the snapshot review section and required fields enforced by `check:release-notes`.
- Updated operator-facing relay troubleshooting guidance in README and in-app FAQ to reflect current recovery actions: `Reconnect`, `Resync`, and `Export diagnostics`.
- Refreshed mobile (`390px`) dashboard visual snapshots for `highlights`, `participants`, and `time-of-day` sections to match the current UI baseline used by release visual gates.

## Why this release matters

- Carries the Phase 3-5 reliability hardening introduced in `v2.3.0`.
- Prevents tagged release workflow failures caused by outdated visual baselines.
- Keeps release and troubleshooting documentation aligned with the enforced CI gates and current relay recovery UX.

## Installation / upgrade notes

- Patch release with no data migration.
- Visual baseline artifacts were intentionally refreshed for mobile snapshot parity.

## Snapshot Review Notes

- Intentional visual diffs: yes
- Notes: Mobile section snapshots were intentionally re-baselined to match current rendering and stabilize release visual gates.
- Reviewer: Engineering

## Included commits

- `f3cf32c` Harden relay diagnostics and guided recovery workflows
- `5df4b19` Harden Phase 4 perf gates and saved-view worker fallback
- `3761e91` phase3: add release-blocking relay/export reliability gates
- `b83a3c3` docs+visual: refresh release guidance and mobile baselines
- `d915572` docs(release): make v2.3.1 notes validator-safe
- `770b83a` chore(release): bump version to 2.3.1
