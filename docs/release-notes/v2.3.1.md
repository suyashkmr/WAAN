# WAAN v2.3.1

## What changed

- Updated docs to clarify tagged release requirements for `## Snapshot Review Notes` and required fields enforced by `check:release-notes`.
- Updated operator-facing relay troubleshooting guidance in README and in-app FAQ to reflect current recovery actions: `Reconnect`, `Resync`, and `Export diagnostics`.
- Refreshed mobile (`390px`) dashboard visual snapshots for `highlights`, `participants`, and `time-of-day` sections to match the current UI baseline used by release visual gates.

## Why this release matters

- Prevents tagged release workflow failures caused by outdated visual baselines.
- Keeps release and troubleshooting documentation aligned with the enforced CI gates and current relay recovery UX.

## Installation / upgrade notes

- Patch release with no data migration.
- Visual baseline artifacts were intentionally refreshed for mobile snapshot parity.

## Snapshot Review Notes

- Intentional visual diffs: yes
- Notes: Mobile section snapshots were intentionally re-baselined to match current rendering and stabilize release visual gates.
- Reviewer: Engineering

## Included commits

- Pending tag cut from `main` after docs + snapshot baseline refresh
