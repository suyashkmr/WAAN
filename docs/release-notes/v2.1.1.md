# WAAN v2.1.1

## What changed

- Removed a non-actionable in-app relay help line that said “macOS launch blocked? See FAQ setup help.”
- Kept Gatekeeper guidance focused in docs/FAQ paths that users can access before launch.

## Why this release matters

- Avoids showing guidance inside the app for a failure mode where the app cannot open.
- Reduces confusion during first launch and keeps support steps channel-appropriate.

## Installation / upgrade notes

- Standard patch upgrade; no data migration required.
- macOS artifacts are produced by `.github/workflows/macos-release.yml` for this tag.

## Included commits

- `ae4edd9` ui: remove non-actionable macOS launch-blocked FAQ hint
