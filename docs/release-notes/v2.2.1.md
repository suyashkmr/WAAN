# WAAN v2.2.1

## What changed

- Updated app branding assets to use the new Gemini-provided logo image.
- Replaced the web dashboard logo and favicon source image (`tpg.png`).
- Replaced the Electron desktop app icon (`electron/icon.png`).
- Bumped root and Electron package versions to `2.2.1`.

## Why this release matters

- Delivers a consistent brand identity across the web UI, browser tab icon, and macOS app icon.
- Keeps release/tag metadata aligned with the shipped artifact version.

## Installation / upgrade notes

- Standard patch upgrade; no data migration required.
- macOS artifacts are produced by `.github/workflows/macos-release.yml` when tag `v2.2.1` is built.

## Included commits

- `990e4a0` chore(branding): update app logo and icons
- `3f42a99` chore(release): bump version to 2.2.1
