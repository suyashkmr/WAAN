# WAAN v2.4.4

## What changed

- Carries forward all `v2.4.3` release changes because the `v2.4.3` macOS release workflow failed and did not publish artifacts.
- Refreshes dashboard visual baseline snapshots used by the release visual regression gate:
  - Highlights section baselines (desktop + mobile).
  - Participants section baselines (desktop + mobile).
  - Time-of-day section baseline (mobile).

## Why this release matters

- Publishes the intended `v2.4.3` payload through a new successful tag after the prior release workflow failure.
- Restores release-gate stability by aligning committed visual baselines with current shipped UI.

## Installation / upgrade notes

- Patch release with no data migration required.
- Existing chats, saved views, and relay state remain compatible.

## Snapshot Review Notes

- Intentional visual diffs: yes
- Notes: Updated only dashboard section baseline snapshots required by current UI layout/spacing.
- Reviewer: Engineering

## Included commits

- `8541ea4` test(visual): refresh dashboard section baselines
- `36a96e5` chore(release): bump version to 2.4.3
- `0b8b354` docs(release): add v2.4.3 release notes
- `6fdb039` Phase 8 complete: finalize checklist and align docs
- `d41e637` Phase 8: remove remaining legacy adapters and activity fallbacks
- `c2c59dc` Phase 8: remove remaining legacy dashboard/search/saved UI fallbacks
- `af9689c` Phase 8: remove legacy search/participant fallbacks and harden Vue mounts
- `14c2540` Phase 8: remove remaining UI fallback render paths
- `b597256` Fix bridge fallback regressions and refresh app logo assets
- `d918135` chore: sync local artifact updates
